image: node:15.4.0

variables:
  PUBLIC_URL: admin-webapp

cache:
  paths:
    - node_modules/

before_script:
  - npm cache clean --force
  - npm install

test:
  stage: test
  script:
    - npm test

pages:
  stage: deploy
  script:
    - npm run deploy
    - cp public/index.html public/404.html
    - mv public _public
    - mv build public
  artifacts:
    paths:
    - public
  only:
    - cosi_dev
